export default function heapSort(arr) {
    const anims = [];
    if(arr.length <= 1) return arr;
    const auxArray = arr;
    heapSortHelper(auxArray);
    return anims;
}

function heapSortHelper(arr, low, high, anims) {
}


